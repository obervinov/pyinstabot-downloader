---
name: Release

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  create-release:
    uses: obervinov/_templates/.github/workflows/release.yaml@v1.2.4
    needs: [build-release-image]

  # milestone:
  #   uses: obervinov/_templates/.github/workflows/milestone.yaml@v1.2.3
  #   needs: [create-release, build-release-image]
