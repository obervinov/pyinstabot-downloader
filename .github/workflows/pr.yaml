---
name: PR

on:
  push:
    branches:
      - '*'
      - '*/*'
      - '**'
      - '!main'

jobs:
  changelog:
    uses: obervinov/_templates/.github/workflows/changelog.yaml@v2.0.1

  pylint:
    uses: obervinov/_templates/.github/workflows/pylint.yaml@v2.0.1

  pytest:
    uses: obervinov/_templates/.github/workflows/pytest-with-vault.yaml@v2.0.1

  pyproject:
    uses: obervinov/_templates/.github/workflows/pyproject.yaml@v2.0.1
  
  pr:
    uses: obervinov/_templates/.github/workflows/pr.yaml@v2.0.1

  build-pr-image:
    uses: obervinov/_templates/.github/workflows/docker.yaml@v2.0.1
    needs: [changelog, pylint, pytest, pyproject]
